# =============================================================================
# PaperLens Environment Variables
# =============================================================================
# Run ./setup.sh to generate your .env file interactively.
# This file documents all available variables — it is not meant to be copied
# directly. Sensitive values are intentionally left blank.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (required) — PostgreSQL connection string
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://paperlens:paperlens_dev@localhost:5432/paperlens"

# -----------------------------------------------------------------------------
# Redis (required) — used for job queue and rate limiting
# -----------------------------------------------------------------------------
REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# Authentication (required) — JWT signing secrets
# Generate strong random secrets: openssl rand -base64 48
# setup.sh auto-generates these for you.
# -----------------------------------------------------------------------------
# JWT_SECRET=
# JWT_REFRESH_SECRET=

# -----------------------------------------------------------------------------
# Azure AI Foundry key (required) + legacy fallback endpoint (optional)
# -----------------------------------------------------------------------------
# AZURE_AI_FOUNDRY_ENDPOINT=
# AZURE_AI_FOUNDRY_KEY=
# AZURE_AI_FOUNDRY_API_VERSION="2025-01-01"

# -----------------------------------------------------------------------------
# Azure Blob Storage (optional) — persistent file storage
# If not set, files are stored locally in .storage/
# -----------------------------------------------------------------------------
# AZURE_STORAGE_CONNECTION_STRING=
# AZURE_STORAGE_CONTAINER="paperlens-files"

# -----------------------------------------------------------------------------
# Admin Seed (optional) — used by `npm run db:seed` to create the first admin
# -----------------------------------------------------------------------------
# ADMIN_EMAIL="admin@example.com"
# ADMIN_PASSWORD=
# ADMIN_NAME="Admin"

# -----------------------------------------------------------------------------
# Application (optional)
# -----------------------------------------------------------------------------
# MAX_UPLOAD_SIZE_MB=50
# NODE_ENV=development
